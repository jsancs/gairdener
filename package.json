{
  "name": "gairdener",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "tsc -b",
    "start": "node --env-file=.env packages/cli/dist/index.js",
    "ui": "npm run build && tsx watch packages/web-server/src/server.ts",
    "check": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "zod": "^3.23.8",
    "@sinclair/typebox": "^0.32.31",
    "commander": "^12.1.0",
    "@types/node": "^22.10.5"
  }
}
